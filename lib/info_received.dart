import 'package:flutter/material.dart';

class InfoReceivedScreen extends StatelessWidget {
  const InfoReceivedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Info Received"),),
    );
  }
}
